# SportsDaily
Making a better and more user friendly sports app that focuses on daily reports. 

Stack:
Python/FastAPI -> backend
expo(react native)/typeScript -> frontend

To run backend:
1. activate venv
2. run: uvicorn main:app --reload

To run frontend:
1. cd into my-app
2. run: npx expo start